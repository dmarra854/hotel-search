# JUnit Platform configuration
# This file takes precedence over the junit-platform.properties bundled
# inside Kafka test jars, eliminating the "Discovered N configuration files"
# warning from the JUnit launcher.
junit.jupiter.execution.parallel.enabled=false
